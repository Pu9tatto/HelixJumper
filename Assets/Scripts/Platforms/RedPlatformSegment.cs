using System.Collections;
using UnityEngine;
using UnityEngine.SceneManagement;

public class RedPlatformSegment : MonoBehaviour
{
    /*[SerializeField] private float _delayForRestart=2;
    public void RestartLVL()
    {
        StartCoroutine(DelayRestartLVL());
    }

    private IEnumerator DelayRestartLVL()
    {
        yield return new WaitForSeconds(_delayForRestart);
        SceneManager.LoadScene(SceneManager.GetActiveScene().name);
    }*/
}
